<?php
namespace Admin\Model;
use Think\Model;
class InformationModel extends Model {
	// 定义自动验证
    protected $_validate    =   array(
    	array('expo_type','require','请输入类型！'),
        // array('year','require','年份必须填写！'),
        // array('description','require','描述必须填写！'),
        );
}